
import { FC } from "react"

const Google:FC = () => {
     
  return (
    <>
    {/* <GoogleLogin
        onSuccess={(res) => {
            const decode = jwtDecode(res.credential)
            console.log(decode);
            
        }}
        allowed_parent_origin={'*'}

        /> */}
        </>
  )
}

export { Google }